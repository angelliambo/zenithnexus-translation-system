/**
 * Commands exports
 */

export { sync, syncAll, syncLanguage, syncUpdate, default as syncDefault } from './sync';
export { detect, detectDeadKeys, cleanDeadKeys, default as detectDefault } from './detect';
export { build, getInfo, default as buildDefault } from './build';
export { show, default as configDefault } from './config-cmd';
